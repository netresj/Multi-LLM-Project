# AGENTS.md

## 共通プロンプト

このファイルを共通ルールの正本とする. 作業前に [共通プロンプト](.llm-agents/instructions.md) も読み、本ファイルと合わせて適用すること. 競合する場合は本ファイルを優先すること.

## 必須事項

- ユーザとの対話, ドキュメント, ソースコードのコメント は日本語を用いること.
- MCP を利用する場合はユーザに承認を求めること.
- 大規模なトークンの利用が想定されるタスクを行う場合はユーザに承認を求めること.

## コーディングエージェントに対するルール

- Claude Code, Codex, Github Copilot を1つのプロジェクトで同時に利用できるようにする.
- `AGENTS.md` を共通ルールの正本として扱うこと. `CLAUDE.md` および `.github/copilot-instructions.md` は `AGENTS.md` を参照しつつ、追加でそれぞれの特有の設定を記載すること.
- 拡張機能ごとに個別にファイルを作らない. シンボリックリンクやファイル内の参照などを活用することで, 核となるプロンプトは `.llm-agents` 内の1ファイルに集約されている状態にすること.
